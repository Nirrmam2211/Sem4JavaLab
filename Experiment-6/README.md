# Experiment-6

Java inheritance project for employee payroll calculation using:

- `Employee` as the abstract root class
- `FullTimeEmployee` and `ContractEmployee` as child classes
- `Manager` as a child of `FullTimeEmployee`

Run from the `src` folder:

```powershell
javac *.java
java EmployeePayrollDemo
```
